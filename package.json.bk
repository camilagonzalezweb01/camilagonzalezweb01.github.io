{
  "name": "vanilla",
  "version": "1.0.0",
  "description": "JavaScript example starter project",
  "main": "index.html",
  "scripts": {
    "start": "parcel main.html --open",
    "build-test": "rm -rf ./dist && cp -R ./carnivores  && cp -R ./pages ./dist && cp -R ./insects ./dist && cp -R ./fish dist && cp -R ./birds ./dist && parcel build index.html",
    "build": "parcel build main.html index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "keywords": [
    "javascript",
    "starter"
  ]
}
